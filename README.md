# Create_GIF
A simple Python tool that converts a sequence of PNG images into an animated GIF using imageio.v3.

📸 PNG to GIF Converter (Python)
 
A simple and lightweight Python tool to convert a series of PNG images into a GIF using imageio.v3.

This script takes multiple images (preferably sequentially named PNG files) and combines them into a smooth animated GIF.

✨ Features

Combines multiple PNG frames into a GIF

Uses imageio.v3 — fast and modern

Adjustable frame duration

Works on any OS (Windows/Linux/Mac)

📦 Requirements

Make sure you have Python installed (3.8 or above recommended).

Install the required library:

  pip install imageio

🚀 Usage
1. Place all your PNG images in a folder
  Ensure they are named in order (example: frame1.png, frame2.png, frame3.png, …).
